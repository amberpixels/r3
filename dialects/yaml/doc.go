// Package r3yaml converts r3 query types ([r3.Filters], [r3.Sorts],
// [r3.PaginationSpec], [r3.Fields]) to and from YAML for configuration files.
// Unlike the JSON dialect's compact f/op/v, it uses human-readable names
// (field/operator/value).
package r3yaml
