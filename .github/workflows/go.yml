name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          # Caches GOMODCACHE and GOCACHE, which is what keeps `just lint` from
          # recompiling the golangci-lint engine inside standardgo on every run.
          # The justfile is in the key because it pins the standardgo version:
          # without it the key never changes, the cache is never re-saved, and a
          # version bump would rebuild from scratch on every run forever after.
          cache-dependency-path: |
            go.sum
            justfile
      - uses: extractions/setup-just@v3

      - name: Lint
        run: just lint

      - name: Test (short, no Docker)
        run: just test-short

  # The floor check runs on its own so it never drags a second toolchain into the
  # ci job's cache. The version comes from the justfile, NOT from go.mod: go.mod is
  # the thing under test, so deriving from it would make the check self-fulfilling -
  # raising the go directive would just install a newer toolchain and pass. Pinning
  # independently means a raised go.mod fails here until the promise is raised too.
  floor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: extractions/setup-just@v3

      - id: pin
        run: echo "go=$(just --evaluate floor_go | sed 's/^go//')" >>"$GITHUB_OUTPUT"

      - uses: actions/setup-go@v7
        with:
          go-version: ${{ steps.pin.outputs.go }}
          cache: false

      # GOTOOLCHAIN=local: use the toolchain just installed and refuse to upgrade past
      # it, so a raised floor is an error here instead of a silent download.
      - run: FLOOR_GO=local just floor

  # Full integration suite: runs the testcontainers-backed tests against real
  # Postgres/MySQL/MongoDB. GitHub-hosted ubuntu runners ship a working Docker
  # daemon, so testcontainers manages its own containers — no `services:` needed.
  # Gated behind `ci` so we don't pull DB images when lint/short fail.
  integration:
    runs-on: ubuntu-latest
    needs: ci
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - uses: extractions/setup-just@v3

      - name: Test (full, with Docker/testcontainers)
        run: just test-integration
